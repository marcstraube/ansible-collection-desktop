---
- name: Converge
  hosts: all
  gather_facts: true

  vars:
    browser_enabled: true
    browser_firefox_enabled: true
    browser_chromium_enabled: true
    browser_librewolf_enabled: false
    browser_brave_enabled: false
    browser_tor_enabled: false
    browser_zen_enabled: false

    browser_firefox_policies_enabled: true
    browser_chromium_policies_enabled: true

    # Per-user paths now bootstrap profiles via filesystem writes,
    # no DISPLAY/D-Bus needed — keep enabled in container tests.
    browser_firefox_user_js_enabled: true

    # Exercise the user.js path explicitly — defaults are now empty so
    # the role would skip the deploy. One sample pref is enough.
    browser_firefox_preferences:
      privacy.resistFingerprinting: true

    browser_default: 'firefox'
    browser_user_config_mode: 'managed'
    browser_users:
      - username: 'browser_test_user'

    # Minimal extensions for testing
    browser_firefox_extensions:
      'uBlock0@raymondhill.net':
        url: 'https://addons.mozilla.org/firefox/downloads/latest/ublock-origin/latest.xpi'
        mode: 'force_installed'

    browser_chromium_extensions:
      'cjpalhdlnbpafiamejdnhcphjbkeiagm':
        mode: 'force_installed'

  tasks:
    - name: Converge | Include browser role  # noqa: role-name[path]
      ansible.builtin.include_role:
        name: '{{ playbook_dir }}/../..'
